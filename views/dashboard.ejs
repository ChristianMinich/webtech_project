<head> 
  <link
      href="https://fonts.googleapis.com/css?family=Lora:400,700|Montserrat:300"
      rel="stylesheet"
    />
  <link href="../public/css/main.css"/>
</head>
<body>
  <%- include('navbar') %>
  <h1>hello there</h1>
  <h2><%= user%></h2>
  <button onclick="logout()">Logout</button>
  <script>
    function logout() {
      var xhr = new XMLHttpRequest();
      xhr.open("GET", "/logout", true); // Relative URL path
      xhr.onreadystatechange = function () {
        if (xhr.readyState === 4 && xhr.status === 200) {
          console.log(xhr.responseText); // Response from the server
        }
      };
      xhr.send();
    }
  </script>
</body>
